# --- Open-Meteo pipeline ---
openmeteo-requests>=1.3.0   # official client (flatbuffers, fast)
requests-cache>=1.2.0       # transparent HTTP caching
retry-requests>=2.0.0       # retry/backoff wrapper
pandas>=2.2.0
pyarrow>=16.0.0             # parquet engine
numpy>=1.26.0
PyYAML>=6.0

# --- IMD pipeline ---
imdlib>=0.1.30             # IMD gridded binary -> xarray/netcdf
xarray>=2024.1.0
netCDF4>=1.6.0

# --- analysis / utilities ---
tqdm>=4.66.0               # progress bars
scipy>=1.12.0              # nearest-grid interpolation, stats

# --- master grid (lat/lon -> state/district) ---
geopandas>=1.0.0           # point-in-polygon spatial join
pyogrio>=0.7.0             # fast GADM .gpkg reader
shapely>=2.0.0
# NOTE: also needs two reference files under data/reference/:
#   gadm41_IND.gpkg (GADM 4.1 India boundaries, ~50MB) -> state + district:
#     curl -L -o data/reference/gadm41_IND.gpkg \
#       https://geodata.ucdavis.edu/gadm/gadm4.1/gpkg/gadm41_IND.gpkg
#   IN.txt (GeoNames India postal dump) -> pincode (nearest centroid):
#     curl -L -o data/reference/IN.zip https://download.geonames.org/export/zip/IN.zip
#     (then unzip IN.txt into data/reference/)
